1. add to legend contorl:
	- multiple icon types including lines and bars
	- control over their size 
	- control over gap from icon to text
	- control over legend element layouts 
2. add to panell control:
	- control over axis lines making a box / or not
3. add to layout control:
	- ability to arbitrarily place panels
	- dynamic gap sizes
	- Axis Tick control
		- Ability to have custom tick layout
		- ability to specify values to be ticked
		- ability to set ticking paddings (for min / max)
		- join and end setting with type enums for all axis line segments
4. add LayerTypes:
	- add Line plots
5. change layerSpec
	- make std::variant for each type of layer and an std::variant of those as a playload for layerSpec
6. Support more dataTypes as vector inputs
	- time/date
	- custom layouts???



GUI app:

- add ability to choose pre made template patterns (pair plot) {Initial plumbing in}
- add conviniance shortcuts, text copy paste, endite layer duplication property copy paste
- CSV parsing input as "mock" data?

- 
- sting input height is tiny on empty defualt text
- column input + button not in middle 
- dataset + icon should have tint on hover + background color.

- pollyline trinagluation can be made better with inner line intersection + normal direction "across point" detection.
- GPU renderer honoring text box Clip functionalty
- Text Layout is simply not the same as CPU renderer
- ? Help with [saved] on the navbar
- 
- data input scrolling not working.
- MultiThreading ???
- add graceful error handling (error showing with [saved] stamp?)
- If FlowUi already has the font loaded from imported tample do no work.
- A lot of buffers in use at destroy request time errors while altering the plot figure.
- props panel hint text hitting the editor wierdly


- undo/redo with dataset value changes need an overview
- undo/redo still compares entire templates instead of specific values.
- resize min max of both template and props panel
- error messages can be more descriptive.
- histogram border lines fall in subpixel ranges
- two column input needs visual overhaul


- label field schema mapping Vlaue has no options (circle, star, diamond.. etc)
- carat movement controlls acros dataset table inputs 
- tab functionality
- icon tints
- border colors
- + and delee button background color need update


